import * as React from 'react';
import ResponsiveToolBar from "./ToolBar";

export default function Header() {
    return(
        <React.StrictMode>
            <ResponsiveToolBar />
        </React.StrictMode>
    );
}
