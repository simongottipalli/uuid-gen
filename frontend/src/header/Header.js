import * as React from 'react';
import AppToolbar from "./Toolbar";

export default function Header() {

    return(
        <React.StrictMode>
            <AppToolbar />
        </React.StrictMode>
    );
}
